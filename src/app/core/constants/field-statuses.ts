import {Cross, Nought, Untouched} from "../interfaces/fieldBox.interface";

export const FIELD_STATUSES = {
  UNTOUCHED:'untouched' as Untouched,
  NOUGHT: 'nought' as Nought,
  CROSS: 'cross' as Cross,
}
